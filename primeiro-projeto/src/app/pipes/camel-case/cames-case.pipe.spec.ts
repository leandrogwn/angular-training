import { CamesCasePipe } from './cames-case.pipe';

describe('CamesCasePipe', () => {
  it('create an instance', () => {
    const pipe = new CamesCasePipe();
    expect(pipe).toBeTruthy();
  });
});
